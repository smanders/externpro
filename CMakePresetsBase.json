{
  "version": 8,
  "configurePresets": [
    {
      "name": "config-base",
      "hidden": true,
      "binaryDir": "${sourceDir}/_bld-${presetName}",
      "cacheVariables": {
        "XP_STEP": "build",
        "XP_DEFAULT": "OFF",
        "XP_PRO_BZIP2": "ON",
        "XP_BUILD_DEBUG": "OFF"
      }
    }
  ]
}
